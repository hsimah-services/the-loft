general = {
  name = "The Loft";
  output_backend = "pipe";
  output_format = "S16";
  output_rate = 44100;
};
pipe = {
  name = "/tmp/snapcast/shairport-fifo";
  audio_backend_buffer_desired_length_in_seconds = 0.0;
};
