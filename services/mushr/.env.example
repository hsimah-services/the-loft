# No environment variables required.
# Before deploying, edit dnsmasq.conf and replace SPACE_NEEDLE_LAN_IP
# with space-needle's actual LAN IP (e.g. 192.168.86.100).
# Find it with: ip addr show | grep 'inet 192'
