# Houstn — combined dashboards/monitoring stack
#
# Compose profiles control which containers run on each host:
#   hub     — beszel (fleet metrics hub) + uptime (Uptime Kuma) + homepage (dashboard)
#   metrics — glances (per-host CPU/RAM/disk API for Homepage)
#
# space-needle (runs the full stack):
#   COMPOSE_PROFILES=hub,metrics
#
# All other hosts (fjord, viking, calavera — glances only):
#   COMPOSE_PROFILES=metrics
#
# beszel and uptime have no required environment variables — their admin accounts
# and configuration are set via the web UI on first access.
#   beszel  → https://beszel.loft.hsimah.com  (or http://beszel.space-needle)
#   uptime  → https://uptime.loft.hsimah.com  (or http://uptime.space-needle)
#
# homepage reads HOMEPAGE_VAR_* values from this file at startup and substitutes
# them into homepage-config/*.yaml. Access at:
#   homepage → https://homepage.loft.hsimah.com (or http://homepage.space-needle)
#
# glances has no required environment variables.

COMPOSE_PROFILES=hub,metrics

# ── Plex ─────────────────────────────────────────────────────────────────────
# Get your token from: Plex Settings → Account → X-Plex-Token (in a signed-in URL)
HOMEPAGE_VAR_PLEX_TOKEN=

# ── Stellarr *arr API keys ─────────────────────────────────────────────────────
# Found in each app under Settings → General → Security
HOMEPAGE_VAR_RADARR_API_KEY=
HOMEPAGE_VAR_SONARR_API_KEY=
HOMEPAGE_VAR_LIDARR_API_KEY=
HOMEPAGE_VAR_BAZARR_API_KEY=

# ── Transmission ──────────────────────────────────────────────────────────────
# Leave blank if you have not set credentials in Transmission
HOMEPAGE_VAR_TRANSMISSION_USERNAME=
HOMEPAGE_VAR_TRANSMISSION_PASSWORD=

# ── Soulseek (slskd) ──────────────────────────────────────────────────────────
# Generate an API key in slskd → Settings → Security → API Keys
HOMEPAGE_VAR_SLSKD_API_KEY=

# ── Uptime Kuma ───────────────────────────────────────────────────────────────
# Create a public Status Page in Uptime Kuma and set its slug here
HOMEPAGE_VAR_UPTIME_KUMA_SLUG=

# ── Sputnik briefing ──────────────────────────────────────────────────────────
# Credentials for briefing.loft.hsimah.com, so the tile on the Briefing tab can
# read the manifest counts. Must match BRIEFING_USER and the password you hashed
# into BRIEFING_HASH in services/mushr/.env — plaintext here, hashed there.
HOMEPAGE_VAR_BRIEFING_USER=loft
HOMEPAGE_VAR_BRIEFING_PASSWORD=
