# kitty config for calavera's rodnik i3 session (deployed by setup.sh §11b).
# The MA dashboard is the primary surface; kitty is the drop-to-terminal admin
# shell (mod+Return). Pin a readable font size for the Surface Pro 2's HiDPI
# panel — bump this if it renders too small/large on hardware.
font_size 11.0
