# space-needle aliases — source from ~/.bashrc or ~/.bash_aliases
alias space-needle-update='cd /srv/space-needle/ && git checkout main && git pull'
alias space-needle-ctl='/srv/space-needle/space-needle'
